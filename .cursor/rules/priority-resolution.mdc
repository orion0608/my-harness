---
description: 流程/文档冲突裁决；第三级要求尽可能使用 AskQuestion、TodoWrite、cursor-ide-browser（网页操作/验证），禁止用文字替代。
alwaysApply: true
---

# 优先级裁决规则

本规则定义 Harness 体系中各规则/体系产生冲突时的裁决顺序。必须在每次决策前检查。

---

## 第一级：Superpowers 流程绝对优先

当 Harness 的任何规则与 **Superpowers 体系** 在**软件开发流程**上产生冲突时，**Superpowers 绝对优先**。

- Superpowers 定义的流程（brainstorming → writing-plans → TDD → code review → verification）不可被 Harness 覆盖或跳过
- Superpowers 的 HARD-GATE（如"未批准设计前禁止编码"）不可被绕过
- Harness 只能在 Superpowers 流程的各个节点**插入补充动作**，不能替代或缩短 Superpowers 流程

当两者不冲突、可以互补时，**同时采纳**。

## 第二级：文档维护规则绝对优先

当多个规则对**文档维护**的要求产生冲突时，**`doc-project-structure` 规则绝对优先**。

- 如果 `doc-project-structure` 要求维护 A 文档，其他规则/技能要求维护 B 文档且两者在同一路径下冲突——以 `doc-project-structure` 为准
- 如果两者要求的文档位于不同目录、无路径冲突——**同时满足**，不构成冲突
- 示例（不冲突）：`doc-project-structure` 要求在 `docs/<branch>/plan.md` 写计划副本，Superpowers 要求在 `docs/superpowers/plans/` 写计划——两者路径不同，同时满足
- 示例（冲突）：`doc-project-structure` 要求 `README.md` 的某个章节为 X 结构，其他规则要求同一章节为 Y 结构——以 `doc-project-structure` 为准

## 第三级：交互式工具优先

与第一、二级并列生效。**宁可多用，不可黑盒**——不为赶进度省略工具。

**尽可能**使用（实际调用工具，勿只在正文里提及）：

| 工具 | 何时用 |
|------|--------|
| **AskQuestion** | 需要用户选择、确认、授权或定方向时；其它规则要求 AskQuestion 时 |
| **TodoWrite** | 多步骤任务：开工前列清单，推进时更新 |
| **cursor-ide-browser** | 凡涉及网页的查看、操作、验证（见下节）；宣称 UI/前端/Web 相关「完成」前须有浏览器证据 |

**禁止**：正文里开放式索选（「你选 A 还是 B？」）、默认替用户做决定、纯文字描述界面或页面行为——应用上表工具替代（用户已说清楚的除外）。

目标：用户看得见进度、选得清方向、瞧得见界面。Skills 与其它 MCP 照常配合使用。

### cursor-ide-browser（内置浏览器 MCP）

涉及**网页**的任务须通过 MCP **`cursor-ide-browser`** 实际执行，不得仅用文字假定页面状态，也不得在未打开页面前声称验证通过。

| 场景 | 必须使用浏览器 |
|------|----------------|
| UI / HTML / Demo 预览与确认 | `browser_navigate` 打开可访问 URL（HTTP 或 Companion URL），`browser_snapshot` 确认布局与内容 |
| 前端功能验证、回归、手工测试 | 在浏览器中走关键用户路径（导航、点击、填表等），变更后重新 `browser_snapshot` |
| Bug 复现与修复确认（Web） | 按复现步骤在浏览器中执行，修复后同路径再验证 |
| 设计/验收中的「能打开、能操作」 | 与 `brainstorming-visual-enhancement`、`preview-html`、Visual Companion 衔接：先获得 **http(s)://** 地址，再 navigate（**禁止** `file://`） |

**不适用**（无需强行开浏览器）：纯后端/API/CLI/单元测试且无 DOM 或浏览器行为；用户明确只要代码审查或静态分析。

**禁止替代浏览器的情形**：

- 需要 DOM、渲染、样式、客户端脚本或用户交互时，用 `curl`/`wget`/仅读 HTML 文件代替浏览器验证
- 未 `browser_navigate`（或等价打开）且未 `browser_snapshot`（或截图）就断言「页面正常」「测试通过」「按钮可用」
- 在回复中长篇描述页面结构或交互，却不调用浏览器工具

**推荐流程**（调用前须查阅该 MCP 工具 schema）：

1. `browser_tabs`（`action: list`）了解已有标签页
2. `browser_navigate` 到目标 URL → 需要交互时 `browser_lock`（`action: lock`）
3. 任何点击、输入、导航等**可能改变页面结构**的操作前/后：以最新 `browser_snapshot` 为结构依据（交互后结构不明则再 snapshot）
4. 宣称完成或向用户汇报 Web 验证结果前：至少一次 snapshot 或 `browser_take_screenshot` 作为证据摘要
5. 本回合浏览器操作结束：`browser_lock`（`action: unlock`）

与 **`preview-html`**、Visual Companion 配合时：先按对应技能/规则启动 HTTP 服务或 Companion，再用本 MCP 打开返回的 URL，不得混用 `file://`。

---

## 裁决流程

```
规则冲突？ → Superpowers（流程）/ doc-project-structure（同路径文档）
无冲突时   → 同时采纳；第三级：AskQuestion / TodoWrite / cursor-ide-browser（网页场景）须实际调用
```
