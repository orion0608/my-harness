---
description: 头脑风暴架构思考增强——在 brainstorming Step 4↔5 判定架构决策时强制加载 architecture-design 技能，补足模块边界、契约、错误模型、测试架构、迁移策略等维度。
alwaysApply: true
---

# 头脑风暴架构思考增强

本规则在 Superpowers brainstorming **Step 4（Propose 2-3 approaches）与 Step 5（Present design）之间**插入架构门禁：判定是否涉及架构决策，命中则读 `architecture-design` 并按其 workflow 产出「架构设计」节内容。不修改 Superpowers 步骤顺序、不替代用户审批。架构维度正文以 `architecture-design` 技能为信源，本规则不重复其 workflow 步骤。

---

## 一、接入点与路由

**判定与门禁仅在 Step 4 完成之后、Step 5 开始呈现任何设计节之前执行**；未命中 [架构决策判定](#架构决策判定) 则一句话说明理由后跳过，直接进入 Step 5。

```
Superpowers brainstorming 原生流程：
  Step 1 Explore project context
  Step 2 Offer Visual Companion（若适用）
  Step 3 Ask clarifying questions
  Step 4 Propose 2-3 approaches
  ─────────────────────────────────────
  【本规则接入点】
    1. 架构决策判定（见下节）
    2. 命中 → 读 `architecture-design` SKILL.md
       → 0-1 / 1-N 路由（见下「命中后动作」）
       → 按第二节交付产物
       → 作为「架构设计」节并入 Step 5
    3. 未命中 → 跳过
  ─────────────────────────────────────
  【与 brainstorming-visual-enhancement 并行】
    两插件均在 Step 4 与 Step 5 之间执行；视觉与架构各自完成后再 Step 5
  ─────────────────────────────────────
  Step 5 Present design
  Step 6 Write design doc
  Step 7-9 Spec self-review / User review / writing-plans
```

### 架构决策判定

当 brainstorming 涉及以下**任一**情况，本插件命中：

- 新建系统 / 新建子系统
- 新增 / 删除 / 重命名模块
- 调整层与层之间的依赖方向
- 新增 / 修改公开 API 或事件契约
- 改变核心数据模型或表结构
- 引入新的外部依赖（DB / MQ / 第三方 SDK / AI 模型供应商）
- 跨模块的代码迁移或重构
- 「是否要重构 / 拆服务 / 分层」的提问
- 同一类业务规则散落，开始讨论收敛

**与 `brainstorming-visual-enhancement`**：判定可叠加。UI 场景按 visual 规则完成方向预览；若同时触及上述任一项，仍须本规则接入 `architecture-design`。两插件各负责视觉 / 架构，不互相替代。

**不命中示例**：纯文案改动、纯样式微调（不变更组件结构）、本地化、配置项调整（不变更配置来源）、文档修订。

### Agent 提示语（命中时须显式告知，不得静默接入）

> 本次涉及架构决策，将按 `architecture-design` 技能展开技术架构维度的设计；不替代 brainstorming 的需求澄清与设计审批流程。

0-1 追加（关键约束项）：

> 0-1：将按 `tech-stack-guidance` 请您选定系统级别并写入 requirement。

### 命中后动作

1. Read `architecture-design` SKILL.md
2. **0-1**：Read `tech-stack-guidance` → AskQuestion（工具级 | 平台级）→ 写入 requirement.md §1 → `workflow-greenfield`
3. **1-N**：从 README / ARCHITECTURE 读已锁定级别并校验 → 不重选级别 → `workflow-iteration`
4. 按 [第二节](#二强制产出) 交付产物，并入 Step 5「架构设计」节，等待用户逐节审批
5. Step 6 落盘见 `doc-project-structure`

**冲突裁决**：与 Superpowers / `doc-project-structure` 冲突时按 `priority-resolution`；本规则只插入产出要求，不跳过、合并或重排 Superpowers 步骤。

---

## 二、强制产出

接入后须写入 brainstorming spec 与 `docs/<branch>/design.md`「架构设计」节（外层格式见 `doc-file-definition-design`）：

| 产物 | 0-1 必填 | 1-N 必填 | 由哪一文件指导 |
|---|:---:|:---:|---|
| 场景判定（0-1 / 1-N） | ✅ | ✅ | `architecture-design/SKILL.md` |
| 目标 / 非目标 / 不变量 / 约束 | ✅ | ✅ | 基本原则 1 |
| 现状调查（模块 / 调用链 / 数据流 / 测试 / issues） | ⬜ | ✅ | `workflow-iteration.md` 步骤 1 |
| 调整判断（不调整 / 局部 / 系统级，**唯一**） | ⬜ | ✅ | `workflow-iteration.md` 14 项清单 |
| 至少 2 个候选方案 + 取舍 + 推荐 | ✅ | △ | `workflow-greenfield.md` 步骤 3 / `workflow-iteration.md` 步骤 4 |
| 模块设计（职责 / 边界 / 公开契约） | ✅ | △ | `reference.md` |
| 分层与依赖规则 | ✅ | △ | `reference.md` |
| 数据访问 / 事务边界 | ✅ | △ | `reference.md` |
| 接口契约（错误模型 / 幂等 / 版本） | ✅ | △ | `reference.md` |
| 测试策略（先于结构调整的顺序） | ✅ | ✅ | `reference.md` |
| 迁移步骤 + 回滚策略 | ⬜ | ✅（分流 B/C） | `workflow-iteration.md` |
| 风险与取舍 + 验收标准 | ✅ | ✅ | `reference.md` |

> △：1-N「不调整」时仅需描述现有结构如何承载；「局部」或「系统级」时必填。

---

## 三、终态门禁（宣称「设计已呈现」前）

- [ ] 已按 [架构决策判定](#架构决策判定) 完成命中/跳过（跳过须有一句话理由）
- [ ] 命中：已告知用户、已 Read `architecture-design` 并完成 0-1 或 1-N 路由与对应 workflow
- [ ] 命中：已按第二节交付产物；用户已批准「架构设计」节；与 visual 插件产出（若适用）未互相覆盖
