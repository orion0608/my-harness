---
description: my-harness-build — Harness 设计原理、操作边界、Cursor 扩展机制要求，以及非 Harness 项目的严格禁止
alwaysApply: true
---

# my-harness-build

> **路径约定**：本文所有以 `.cursor/` 开头的相对路径，均指 **项目顶级目录** `my_harness` （my_harness是项目根目录，也是项目名称）下的 `.cursor/` 子目录。其他项目的 `.cursor/` 目录不在本文约定范围内。

## 一、Harness 设计原理

Harness 是对 **Superpowers 插件体系** 的增强层，通过在项目级别叠加 Rules、Skills、Commands、Subagents、Hooks，弥补 Superpowers 在文档管理、触发方式、可视化沟通方面的不足。

**核心约束：不改动 Superpowers 任何内容，仅在本项目范围内新增/修改/删除。**

全部设计围绕以下原则展开：

### 1. 三体系架构

Harness 由三个协作体系构成；职责、引用边界与冲突裁决均在本节定义。

#### Superpowers 体系 — 软件开发流程

- **职责**：定义软件开发流程（brainstorming → plan → implement → review …）。
- **第一原则**：绝不修改 Superpowers 目录内任何内容（见 §四）。
- **插件扩展**：Harness 以 `*-enhancement` 规则在 Superpowers 某技能的**固定步骤之间**做非冲突插入；须写明插入时机；不重写触发条件、不改顺序、不替代模板。
- **插件原则**：短、单插入点、单职责；正文宜控制在数十行量级，只写**接入点**、**动作**、必要时**记录**（如何落盘）；不得把其它阶段的长规范默认套到每个插件上。
- **插件指向**：插件动作可指向 Superpowers 具体环节，也可指向 Doc 体系具体文档（仅名词引用：`doc-project-structure`、`doc-file-definition-*`）；过程文档不重复格式定义。
- **插件与 Commands**：Commands 不复述 `*-enhancement` 插件正文；插件细节由规则自身（通常 `alwaysApply`）在流程执行时生效。
- **新建插件**：先读目标 Superpowers 技能并标出唯一插入点 → 编写对应 `*-enhancement` 规则 → 按 §七更新 `Dependency-Relationship-Map`（目标技能名与插入点）。

#### Doc 体系 — 项目文档架构

- **范围**：`doc-project-structure` 规则 + 全部 `doc-file-definition-*` 技能。
- **允许内部引用**：体系内规则与技能可互相引用（如 A 文档格式引用 B 文档字段）。
- **允许环节引用**：维护时机可用 Superpowers **业务环节**的模糊名词（或superpowers体系某环节等），不绑定具体技能文件名。

  > ✅ "头脑风暴完成、设计定稿后"
  > ❌ "brainstorming 技能执行完毕后"

- **禁止外部引用**：Doc 体系规则/技能**不得**引入 Doc 体系外的任何技能或规则作为触发或执行依赖（如「在某某技能第 N 步读取本技能」）。格式字段的来源用业务语义描述，不用外部组件名绑定。

#### Commands 体系 — 流程触发器

- **职责**：让用户更好触发 Harness。
- **流程类 Command**：直接触发引用 Superpowers 的环节性技能；对文档维护只要求遵循 `doc-project-structure`（决策矩阵、必写/选写、触发时机），不在 Command 中引用具体 `doc-file-definition-*` 或复制文档路径/格式。
- **格式查询类 Command**：文档格式展示（如 `/docs-regulation-show`）可读取 `doc-file-definition-*`，不属于流程类文档维护声明。
- **非编程流程**：元数据/运维类子流程（如 `/lesson-record`）可在 Command 中承载细节；**新增**此类 Command 须用户明确批准。

#### Harness 扩展层自包含

Rules、Commands、非 Doc 类 Skills 不引用 Superpowers 等**项目外**路径；引用同项目组件时仅使用名词（见 §3）。Doc 体系引用边界见上「Doc 体系」。

### 2. 信源唯一性（Single Source of Truth）

各体系边界见 §1。每一类知识有且仅有一个存放位置：

| 知识类型 | 唯一源头 | 存放位置 |
|---------|---------|---------|
| 文档目录结构、Git 追踪策略、决策矩阵 | `doc-project-structure` 规则 | `.cursor/rules/` |
| 每个文档的格式、模板、内容要求 | `doc-file-definition-*` 技能 | `.cursor/skills/` |
| 项目 UI 风格决策（美学模式、slug、实现方式；合入后全局） | `doc-file-definition-architecture` §8；分支过程见 `doc-file-definition-design`「前端设计」 | `docs/ARCHITECTURE.md`；`docs/<branch>/design.md` |
| UI 实现路由（参考索引、ui-frame、free-style） | `frontend-design` 技能 | `.cursor/skills/frontend-design/` |
| 技术架构设计（0-1 初建 / 1-N 迭代判断 / 模块、依赖、契约、错误、测试、迁移） | `architecture-design` 技能 | `.cursor/skills/architecture-design/` |
| 组织技术栈与系统开发模式（工具级/平台级约束） | `tech-stack-guidance` 技能 | `.cursor/skills/tech-stack-guidance/` |
| 架构思考接入 Superpowers brainstorming 的方式 | `brainstorming-architecture-thinking-enhancement` 规则 | `.cursor/rules/` |
| Superpowers 流程插件（方法论；命名 `*-enhancement`） | §1「Superpowers 体系」；各插件插入点与动作见对应 `*-enhancement` 规则 | `.cursor/rules/` |
| 本地 HTML HTTP 预览与内置浏览器打开 | `preview-html` 技能 | `.cursor/skills/preview-html/` |
| Windows 上 Visual Companion 启动与屏写 | `visual-companion-windows` 技能 | `.cursor/skills/visual-companion-windows/` |
| 用户触发入口、工作流步骤 | Commands | `.cursor/commands/` |
| 软件开发流程 | Superpowers 技能 | `C:\Users\orion\.cursor\plugins\local\superpowers\` |
| 全局优先级裁决 | `priority-resolution` 规则 | `.cursor/rules/` |
| Agent 对外沟通风格（精简、去噪、精确优先） | `concise-communication` 规则 | `.cursor/rules/` |
| Harness 修改流程（影响分析、两级审批、修改后联动） | 本节 **§七** | 同文件 |

**任何信息不得在两处同时定义。** Skills 不复制决策矩阵，Rules 不复制文档格式；Commands 文档维护与插件边界见 §1。

### 3. 动态引用机制

Rules、Commands、Skills 之间如需相互引用，**只能引用目标的名词**（规则名、技能名、命令名），不得通过文件路径进行引用。

> ✅ "详见 `doc-project-structure` 规则"
> ❌ "详见 `.cursor/rules/doc-project-structure.mdc`"

这样当目标文件的存放位置或文件名发生变化时，引用方无需修改。


---

## 二、Cursor 扩展机制要求

Harness 使用 Cursor 的五种扩展机制。创建和维护时遵循对应内置技能规范：

| 扩展类型 | 存放位置 | 格式要求 | 创建规范 |
|---------|---------|---------|---------|
| **Skills（顶层）** | `.cursor/skills/<name>/SKILL.md` | YAML frontmatter + Markdown 正文 | 遵循 `create-skill` 内置技能 |
| **Skills（子技能）** | `.cursor/skills/<parent>/<sub>/SKILL.md` | 同上 | 由父技能目录表登记；仅允许在已定义的父技能下扩展 |
| **Rules** | `.cursor/rules/<name>.mdc` | YAML frontmatter（`description`/`globs`/`alwaysApply`）+ Markdown | 遵循 `create-rule` 内置技能 |
| **Commands** | `.cursor/commands/<name>.md` | Markdown 文件，有标题（`#`）和内容即可 | 无需复杂规范 |
| **Subagents** | `.cursor/subagents/<name>.md` | YAML frontmatter（`name`/`description`）+ Markdown 正文（system prompt） | 遵循 `create-subagent` 内置技能 |
| **Hooks** | `.cursor/hooks.json` + `.cursor/hooks/` | JSON 配置文件 + 命令脚本或 prompt hook | 遵循 `create-hook` 内置技能 |

创建或修改以上任何扩展时，Agent **必须先行读取对应的内置技能文件并遵循其规范**。


## 三、操作边界 — 所有修改仅限于此目录

`d:\code\cursor\my_harness\` 是本 Harness 项目的根目录，一切操作**严格限定在此目录范围内**：

| 可操作 | 禁止操作 |
|--------|----------|
| `.cursor/rules/*.mdc` | `C:\Users\orion\.cursor\` 下的任何文件 |
| `.cursor/skills/*/SKILL.md`（顶层技能） | `C:\Users\orion\.cursor\plugins\` 下的任何文件 |
| `.cursor/skills/*/*/SKILL.md`（子技能） | `C:\Users\orion\.cursor\skills-cursor\` 下的任何文件 |
| `.cursor/commands/*.md` | Superpowers 目录下的任何文件 |
| `.cursor/subagents/*.md` | 其他项目目录下的任何文件 |
| `.cursor/hooks.json`、`.cursor/hooks/*` | |
| 项目根目录的 `README.md`、`Dependency-Relationship-Map.md` 等 | |

## 四、严禁行为

1. **禁止修改 Superpowers**：`C:\Users\<userName>\.cursor\plugins\local\superpowers\` 下的内容只读，绝不修改
2. **禁止修改用户级配置**：`C:\Users\<userName>\.cursor\` 下的 rules、skills、commands、hooks 等用户级内容，绝不修改
3. **禁止修改内置技能**：`C:\Users\<userName>\.cursor\skills-cursor\` 下的内容，绝不修改
4. **禁止修改外部项目**：其他项目目录，绝不修改
5. **非 my-harness 项目严格禁止创建扩展**：在任何非 `my_harness` 的项目中，严禁创建 skills、rules、commands、subagents、hooks。这些 Cursor 扩展机制仅允许存在于 Harness 项目自身范围内，不得向其他项目泄漏或复制。

## 五、唯一可操作范围

```
d:\code\cursor\my_harness\
├── README.md
├── .gitignore
└── .cursor/
    ├── rules/
    ├── skills/
    ├── commands/
    ├── subagents/
    ├── hooks.json
    └── hooks/
```

以上目录之外，均为禁区。

## 六、test-project 目录

`test-project/` 目录用于存放 Harness 验证所用的实际测试软件项目。

- 该目录**不参与 Git 跟踪**（已加入 `.gitignore`）
- 需要验证 Harness 功能时，将具体的测试软件项目放入此目录下
- **用户要求创建测试项目时**，必须在此目录下创建（如 `test-project/<project-name>/`），不得在其他路径新建用于 Harness 验证的项目
- **开始在某子项目上验证 Harness 时**，使用 `/myharness-test-project <path>` 激活逻辑工作区（`<path>` 必须在 `test-project/` 下）
- 该目录仅供 Harness 开发者验证使用，不影响 Harness 本身的代码结构

### 工作区约束（Harness 验证有效性）

验证 Harness 时，Cursor 工作区根目录必须保持为 **`my_harness` 项目根**，以便 Rules、Skills、Commands 等扩展被正确加载。对 `test-project/` 下子项目的操作采用**逻辑工作区**方式，不得在物理上切换工作区：

| 允许 | 禁止 |
|------|------|
| 在对话与上下文中明确「当前逻辑目标」为 `test-project/<name>/`，读写、Shell、Git 等操作以该路径为 cwd | 使用 `move_agent_to_root`、`move_agent_to_cloned_root`、`create_project` 等将 Agent 工作区根目录迁出 `my_harness` |
| 使用绝对路径或相对于 `my_harness` 的路径访问测试项目文件 | 在 `test-project/` 外单独打开子项目作为 Cursor 工作区进行 Harness 验证 |

**原因**：工作区根目录离开 `my_harness` 后，本项目的 `.cursor/` 扩展不会被加载，验证结果无效。

### 跨项目隔离（强制）

激活某测试子项目的逻辑工作区后（`<logical-root>` 为 `test-project/<name>/`），**严禁跨项目读取** `test-project/` 下其它子项目的内容。各测试项目彼此独立，不得作为当前开发的参考源、上下文或代码来源。

| 允许 | 禁止 |
|------|------|
| 读写 `<logical-root>` 内的文件与 Git 历史 | 使用 Read、Grep、Glob 等读取 `test-project/<其它项目>/` 下的任何文件 |
| 列出 `test-project/` 顶层目录名（用于切换或 `/myharness-test-project` 选项目） | 在 Shell 中 `cd`、`cat`、`type` 等访问其它测试子项目路径 |
| 读写 `my_harness` 根下的 Harness 元数据（`.cursor/`、`README.md` 等） | 将其它测试项目的代码、文档、配置当作当前项目的实现依据或模板 |
| 用户明确要求对比或切换子项目时，按新指令执行 | 为「找参考实现」而主动浏览、搜索其它测试子项目 |

**原因**：测试项目用于隔离验证不同场景；跨项目读取会污染上下文、混淆需求边界，并导致 Agent 误用无关项目的约定或半成品代码。

---

## 七、修改 Harness 的检查

本节是 Harness 元数据修改流程的**唯一操作信源**（先设计、后改动）。

**绝对要求**：对 Harness 本身的任何改造，**无论用户指令是否清晰、是否看似只需改一处**，均须先完成下列步骤并获用户**明确确认**后，方可编辑任何 Harness 元数据文件。不得未经确认直接动手。

**无例外**：不得以「用户已说清楚」「只改一个文件」「任务简单」等理由跳过确认步骤。

**适用范围**：`.cursor/` 下 Rules、Skills（含子技能）、Commands、Subagents、Hooks，以及 `README.md`、`Dependency-Relationship-Map.md` 等 Harness 元数据。

### 修改前（硬性门禁）

须按序完成下列步骤。本门禁优先于「修改前（决策）」自问项。

#### 步骤 1：影响分析

查阅 `Dependency-Relationship-Map`，结合泛性依赖与指定依赖，推断本次改动可能波及的文件（含 README、help、关联命令/规则等）。

#### 步骤 2：编制调整列表

向用户展示下表（可 Markdown 表格）；用户未确认前**禁止** Write / StrReplace / Delete 等写操作。

| 列 | 必填内容 |
|----|---------|
| **目的** | 本次要解决什么、与哪条缺口或诉求对应 |
| **路径** | 拟**新增 / 修改 / 删除**的每个文件或组件名（完整相对路径） |
| **调整方向** | 每个路径一句话说明改什么、不改什么（不写全文草稿亦可） |
| **明确不改** | 用户可能误以为会动到的路径或范围，声明本次不动 |

#### 步骤 3：一级审批（范围确认）

用 **AskQuestion**（`allow_multiple: true`）将调整列表中每个拟改路径列为独立选项；每项须含：**路径**、**类型**（Rule / Skill / Command / Subagent / Hook / 元数据文档）、**修改等级**（必须 / 建议）、**修改内容简述**。用户勾选本次实际要改的项；未勾选项不得改动。不得用「同意整表」「指令清晰」等笼统确认代替逐项勾选。

**设计一致性反馈**：若 Agent 判定用户勾选或原始指令与 Harness 整体设计理念（§1 三体系架构、信源唯一性等）不符，须**显式说明偏离点、风险与更符设计理念的替代**，并用 **AskQuestion** 请用户二次确认（维持原方案 / 采纳建议 / 取消该项）；不得静默执行。

#### 步骤 4：二级审批（内容确认）

对一级审批通过的**每个文件**，**逐个**展示具体拟改内容，包括但不限于：涉及章节/段落、改前要点、改后草案或 diff 级说明、明确不改的段落。

用 **AskQuestion** 请用户对该文件单独确认，选项至少包含：**通过** / **需修订后再审** / **跳过本文件**。

- **通过**：可按展示内容编辑该文件
- **需修订后再审**：Agent 修订展示内容后，对该文件重新发起二级审批
- **跳过本文件**：本次不改动该文件（即使一级已勾选）

**须全部通过二级审批**（或修订后重新通过）后方可进入执行。**禁止**将多个文件的详细改动合并为一次 AskQuestion。

#### 步骤 5：确认后执行

仅编辑二级审批通过的内容；若执行中发现需增删路径或变更拟改内容，**暂停**并回到步骤 2 重新走两级审批。

#### 禁止

- 未展示调整列表即修改 Harness 元数据
- 将「用户指令清晰」等同于「范围已确认」或「内容已确认」
- 未完成二级审批即编辑任一文件
- 一级审批用笼统确认代替逐项勾选；二级审批将多文件内容合并为一次确认
- 用一级审批代替二级内容确认
- 默认顺带修改列表外的 README、help、依赖图等（除非列表中已列出且已通过两级审批）

### 修改前（决策）

用户确认后，每次新增或改动 Harness 组件前，Agent 还须自问：

1. **是否冲突**：是否和现有的流程体系（含插件子流程）、文档体系冲突？存在冲突时必须禁止继续，如果有合理的解决方案可以一并给出。
2. **是否必要**：该改动是否解决真实缺口？能否用既有规则/技能/命令覆盖，而非再叠一层？
3. **是否引发二义性**：措辞、触发条件或与其它组件的边界是否会让 Agent 或用户产生多种合理解读？信源是否仍唯一？
4. **能否精确简化**：在保持语义无歧义的前提下，能否用更短、更少的条目或引用替代冗长方案？（与 `concise-communication` 一致：精确优先，不为简短而损义）
5. **若为 Superpowers 流程插件**（§1）：是否已先定位 Superpowers 插入点？是否避免在 Harness 重写触发条件、缺口长表、以及在 Command 中复述插件正文？
6. **若为新增非编程流程 Command**：是否已获用户明确批准？

五项均不满足时，应暂停改动、向用户说明理由，或先收敛方案再动手。

### 修改后（联动）

每次修改 Harness 项目内的任何内容后，Agent 必须执行以下两项检查：

### 1. 检查依赖关系图是否需要更新

阅读 `Dependency-Relationship-Map` 文档，判断本次修改是否导致依赖关系发生变化（如新增了引用关系、移除了旧引用、组件重命名等）。如有变化，同步更新该文档。

### 2. 检查关联文档是否需要同步更新

根据 `Dependency-Relationship-Map` 文档中记录的依赖关系，判断本次修改是否触发了关联文档的同步义务。如有，逐一完成对应的联动更新。
