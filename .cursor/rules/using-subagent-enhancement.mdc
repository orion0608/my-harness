---
description: Subagent 使用增强——plan 交接后默认 Subagent-Driven，不再询问是否启用 subagent。
alwaysApply: true
---

# Subagent 使用增强

补充 Superpowers **`writing-plans`** 的 Execution Handoff：有实现计划且进入执行时，**默认** Subagent-Driven，**不再** AskQuestion「是否使用 subagent / 子代理」。不改 Superpowers 技能步骤顺序与子代理 prompt 模板。

## 接入点

**`writing-plans` → Execution Handoff**（原步骤 *Which approach?* 之前）：

```
Plan 已保存
  → 声明使用 Subagent-Driven Development
  → REQUIRED：读取并执行 subagent-driven-development（含其 Red Flags）
  → 禁止为「是否启用 subagent」再 AskQuestion
```

其它流程若出现「是否用 subagent」的确认，且本会话未命中下方例外，同样默认 subagent、禁止重复询问。

## 边界

- 本规则插件不影响其它superpowers规定的行为
- 本规则插件仅替用户声明需要强制开启**subagent-driven**，这种声明等同于用户发出的指令，千万重视！
