---
description: 头脑风暴可视化预览步骤默认约束。
alwaysApply: true
---

# 头脑风暴可视化增强

接入点见 **§一**；凡 **Visual Companion** 见 **§二**。不改编排顺序；Step 5 须含已确认可视化产物；Step 6 前不得完整 `design.md`。

**术语**（禁止混用「方案」）：**设计说明** = `design.md` 文字；**结构图** = Mermaid 等（**不能**替代 UI **方向预览**）；**方向预览** = 浏览器确认 UI 大方向；**高保真 Demo** = 可选第二阶段；**备选方向** = 各需预览或差异列表。

---

## 一、流程与判定

本规则在 Superpowers brainstorming 中 **两处插入**。

### 插入点（信源）

```
Superpowers brainstorming 原生流程：
  Step 1 Explore project context
  Step 2 Offer Visual Companion
    scene_UI? → 【Harness 插入 A】零打断，Companion 通道默认启用（§二）
    否则     → Superpowers 独立征询，等待回复
  Step 3 Ask clarifying questions
  Step 4 Propose 2-3 approaches
  ─────────────────────────────────────
  【Harness 插入 B · Step 4↔5】（Step 5 Present design 之前）
    1. 场景判定（见下表）→ §三 UI 或 §四 非 UI
    2. 可视化验收（用户确认；UI 含阶段一方向预览，可选阶段二高保真）
    3. 产物并入 Step 5 各节（UI 阶段二仍在本插入内完成）
  ─────────────────────────────────────
  【与 brainstorming-architecture-thinking-enhancement 并行】
    两插件均在 Step 4 与 Step 5 之间执行；各自完成后再 Step 5
  ─────────────────────────────────────
  Step 5 Present design（须已含经确认的可视化产物）
  Step 6 Write design doc（见 doc-project-structure）
  Step 7–9 Spec self-review / User review / writing-plans
```

- **插入 A（Step 2）**：仅 `scene_UI=true`；不发「Want to try it?」、不 AskQuestion 工具、不等待 opt-in；Companion 按 **§二**。
- **插入 B（Step 4↔5）**：命中任一场景时执行；未命中则跳过 Step 5。

### 场景

| ID | 判定 |
|----|------|
| `UI` | 布局、组件、样式、交互任一变更 |
| `FLOW` | 多步骤用户/系统交互流 |
| `ARCH` | 模块、依赖、部署结构 |
| `DATA` | 表、字段、实体关系 |
| `COMPARE` | ≥2 条需抉择路线（常与上叠加） |

**叠加**：低要求交付物不可替代 `UI` 方向预览。`UI`+`COMPARE`：仍做方向预览；确认后用图/表补非布局对比。

**早期 UI**：Step 1 后符合 `UI` → `scene_UI=true`。

**Step 2（`scene_UI=true`）**：零打断；Companion 通道默认启用（**§二**）。Step 1–3 用户**明确**拒浏览器 → opt-out 文本通道。Step 4↔5 履行方向预览。`scene_UI=false` 时 Step 2 仍按 Superpowers 征询；用户同意后 Companion 亦走 **§二**。

**路由**（冲突自上而下）：`UI` → §三；非 UI → §四；设计说明仅在可视化/方向预览确认后。

**终检**（宣称「设计已呈现」前）：

- **UI**：[] 方向预览已按 **§二** / `preview-html` 验收或已记录降级 [] 已问高保真 Demo [] `design.md`「前端设计」与 ARCHITECTURE §8 一致（1-N 已整表继承，或 0-1/变更项已与用户确认）[] 若改项目级 UI 规范，合入前 `ARCHITECTURE.md` §8 与 `design.md` 一致
- **非 UI**：[] 结构图已确认；设计说明在可视化之后

**Step 5/6**：禁止合并跳过；门禁未过禁止完整 `design.md` 或宣称设计已定稿。

---

## 二、Visual Companion

**触发**：本规则内凡使用 Visual Companion——含插入 A 默认通道、插入 B 阶段一方向预览、Superpowers Step 2 用户同意后的 Companion 可视化。

| 须做 | 说明 |
|------|------|
| **Read `visual-companion-windows`** | 启动、写屏、获得 Companion `url`、失败降级：**以该技能为唯一操作信源**，本规则不重复 |
| **Read `preview-html`** | 仅不走 Companion 的本地 HTML（技能内降级 `direction-*.html`、§三 高保真 Demo 等） |
| **Read `cursor-ide-browser-usage`** | 获得 **http(s)://** URL 后打开并浏览器验收；**以该技能为 MCP 工具序唯一信源**，本规则不重复 navigate / lock / snapshot 逐步表 |

**禁止**：未 Read `visual-companion-windows` 即启动 Companion 

---

## 三、UI 路径（`UI` / `scene_UI`）

不可跳过阶段一（插入 B）。阶段一按 **§二** 完成方向预览 → 用户确认 → 可选阶段二。

**AskQuestion 仅用于**：展示数量、方向预览确认、是否高保真 Demo、前端设计（非 Companion/HTML 工具选择）。

### 展示数量（UI 与非 UI 共用）

| 模式 | UI 方向预览 | 非 UI 图 |
|------|-------------|----------|
| **多预览并排** | 2～3 份可辨认预览 | 2～3 个 Mermaid/Canvas 变体 |
| **单一聚焦** | 1 份预览；其余仅差异列表 | 1 图；其余差异列表（`ARCH` 可先全景再局部） |

**禁止**把「单一聚焦」当成「一图/一文列尽各备选」。

### 阶段一：方向预览

**方向预览 Demo 技术栈**（制作预览页时）：

- **默认**：不引入外部依赖；用 HTML、CSS、原生 JS 呈现基础布局与关键交互即可
- **可选**：若基于现有项目前端代码微调能更清晰传达方向，Agent 可自行选用；非必须

1. 按 **§二**（Companion 或 `preview-html` 获得 URL）→ **Read `cursor-ide-browser-usage`** 完成浏览器验收
2. 约束维度（可与预览迭代）：布局、组件结构、关键交互、响应式、色调风格 → **UI 方向简报**（可附结构图，**不能**替代浏览器预览）
3. 用户**明确确认** → 才可阶段二

### 阶段二：高保真 Demo（须问）

确认后问是否制作 `docs/<branch>/demo/` 高保真页。**需要**：先 [前端设计](#frontend-stack-ui) → `frontend-design` → 写入 demo → **§二** 之 `preview-html` 验收。**不需要**：归档阶段一产物。高保真**不能**替代阶段一。

### 前端设计 {#frontend-stack-ui}

进入阶段二或写 `design.md` 前，若无「前端设计」节 → AskQuestion：**美学模式**、**UI 实现方式**（见 `frontend-design`）。

**「前端设计」落盘**（流程层负责，子技能不重复读文件）：

1. **1-N / 已有 UI 规范**：若 `docs/ARCHITECTURE.md` §8 已存在且本次**不变**项目级 UI 规范 → **Read** §8「前端与 UI 规范」**整表**（美学模式、界面风格、色系、设计参考、UI 实现方式、动效强度、动画库等；字段以 `doc-file-definition-design`「前端设计」为准），**继承**写入本分支 `design.md`「前端设计」（可注「沿用 ARCHITECTURE §8」）；**不重复**征询已继承项
2. **0-1 或变更 UI 规范**：设计/方向预览中已向用户确认的前端相关决策（含美学模式、界面风格、色系、slug、实现方式等）→ **必须**写入 `design.md`「前端设计」对应列，不得仅留对话
3. **开放式**且无既有 §8：按 `doc-file-definition-design` 必填项填写（含界面风格、色系）

调用 `frontend-design` 及子技能前，须已具备完整「前端设计」表（含继承）；由流程注入上下文，不在子技能内再规定读 `design.md`。

写入 `design.md`（`doc-file-definition-design`）；合入 `/accept` 时 §8（`doc-file-definition-architecture`）。`ARCHITECTURE.md` §8 已存在且本次不变 UI 规范时可少问美学模式/实现方式。禁止因无 `package.json` 默认真 HTML。

### 禁止（UI，全文唯一）

- ❌ 用文字简报 / Mermaid / 结构图结束阶段一，替代**方向预览**
- ❌ 方向预览未确认：阶段二、完整 `design.md`、宣称「设计已呈现」
- ❌ 跳过阶段一直接问高保真 Demo
- ❌ 违反 **§二**（含未 Read 技能、Step 2 征询工具、未按技能尝试即降级 HTML）
- ❌ 以「Superpowers 优先」在 Step 2 重复工具征询

命中后：本节 → Step 5 分节呈现 → Step 6 落盘 spec/`design.md`，归档 `direction-*.html` 与高保真 Demo（若有）。

---

## 四、非 UI 路径

命中 `FLOW` / `ARCH` / `DATA` 或**不含** `UI` 的 `COMPARE` 时走本节（`UI` 叠加时布局类对比仍走 §三，非布局对比可用下表 `COMPARE` 行）。

### 场景与展示工具

| 场景 | 推荐工具 | 常用交付物 | 呈现 |
|------|----------|------------|------|
| `FLOW` | **Mermaid** | `flowchart` / `graph` 流程图；跨角色用 `sequenceDiagram`；步骤多时可用 `journey` | 对话中渲染；节点中文，标出决策点（菱形） |
| `ARCH` | **Mermaid** | `graph` 模块/部署关系；`sequenceDiagram` 调用链；数据流经 `flowchart` | 对话中渲染；复杂架构可先全景再局部（§三「单一聚焦」分步） |
| `DATA` | **Mermaid** | `erDiagram` 实体关系 | 对话中渲染；标关键实体、主外键与基数 |
| `COMPARE`（无 `UI`） | **Mermaid** 优先；多维度量化对比用 **Canvas** | 决策矩阵、对比表图、权衡示意图 | Mermaid 在对话中；Canvas 见内置 `canvas` 技能（`.canvas.tsx`） |

**流程**：展示数量 AskQuestion（§三 表，非 UI 列）→ 按上表出图 → 用户确认 → 再写设计说明（`COMPARE` 结论写入 `design.md`）。

**禁止**：用纯文字列表替代应可视化的 `FLOW`/`ARCH`/`DATA`；未确认结构图前写完整设计说明。
