---
description: 头脑风暴 Step 4↔5 可视化门禁；UI 方向预览默认 Visual Companion，启动失败降级 HTML + preview-html。
alwaysApply: true
---

# 头脑风暴可视化增强

本规则在 Superpowers brainstorming **Step 4（Propose 2-3 approaches）与 Step 5（Present design）之间**插入可视化门禁。不修改 Superpowers 步骤顺序；门禁通过后产物并入 Step 5 逐节呈现；Step 6（Write design doc）须待 Step 5 验收通过后再执行。具体工作流见第三、四节。

---

## 一、术语定义（全文统一使用，禁止混用「方案」统称）

| 术语 | 含义 | 典型交付物 | 能否单独满足前端/UI 的可视化义务 |
|------|------|------------|----------------------------------|
| **设计说明** | 写入 `design.md` 的文字结论（背景、决策、风险等） | Markdown 正文 | ❌ 不能 |
| **结构图** | 辅助理解的示意图，非可交互 UI | Mermaid 流程图/架构图/ER 图、组件关系图 | ❌ 不能（前端/UI 场景） |
| **方向预览** | 第一阶段须在浏览器中确认的 UI 大方向 | 默认 Visual Companion；失败时 `docs/<branch>/demo/direction-*.html` + `preview-html` | ✅ 能（见第三节） |
| **高保真 Demo** | 第二阶段可选的、接近最终实现风格的 Demo 页面 | `docs/<branch>/demo/*.html`（见第三节阶段二） | ❌ 不能替代方向预览 |
| **备选方向** | 多个可选 UI/技术路线之一 | 每个备选方向各需一份**方向预览**（多预览并排）或一份预览 + 其余仅列差异点（单一聚焦） | 见 [三、1.2](#步骤-12--确定展示数量askquestion仅影响预览份数) |

> **硬性边界**：场景 `UI` 的可视化义务与禁止行为见 [二、路由与禁止](#路由与禁止) 与 [三、阶段一](#阶段一方向预览必选验收门槛)。

---

## 二、接入点与路由

**判定与门禁仅在 Step 4 完成之后、Step 5 开始呈现任何设计节之前执行**；未命中任一 [场景判定](#场景判定) 则跳过，直接进入 Step 5。

```
Superpowers brainstorming 原生流程：
  Step 1 Explore project context
  Step 2 Offer Visual Companion（若适用；仅 Companion 工具同意，非 UI 方向预览门禁）
  Step 3 Ask clarifying questions
  Step 4 Propose 2-3 approaches
  ─────────────────────────────────────
  【本规则接入点】
    1. 场景判定（见下表）
    2. 路由与禁止（见本节）
    3. 可视化验收（用户确认；第三节或第四节）
    4. 产物并入 Step 5 各节；场景 UI 阶段二仍在 Step 4↔5 内
  ─────────────────────────────────────
  【与 brainstorming-architecture-thinking-enhancement 并行】
    两插件均在 Step 4 与 Step 5 之间执行；各自完成后再 Step 5
  ─────────────────────────────────────
  Step 5 Present design（须已含经确认的可视化产物）
  Step 6 Write design doc（见 doc-project-structure）
  Step 7-9 Spec self-review / User review / writing-plans
```

### 场景判定

| 场景 ID | 判定标准 |
|---------|---------|
| `UI` | 页面布局、组件结构、样式、交互行为任一发生变化 |
| `FLOW` | 多步骤用户操作流或系统交互流 |
| `ARCH` | 模块新增/删除、依赖关系、部署结构 |
| `DATA` | 表结构、字段、实体关系 |
| `COMPARE` | 存在 2 个及以上需用户抉择的可选路线（常与 `UI`/`ARCH` 等叠加） |

**场景叠加**：不得用低要求场景的交付物替代高要求场景（路由见下）。

**Step 2 分工**：Step 2 仅 Companion 工具征询；场景 `UI` 阶段一默认 Companion（§七），不另 AskQuestion 选工具。

**Step 5 / 6 边界**：禁止将 Step 5 与 Step 6 合并跳过；禁止在门禁未过前宣称「设计已呈现」或写入完整 `design.md`。

### 路由与禁止

发生冲突时，**自上而下优先**：

```
1. 前端/UI（场景 UI）→ 必须走 [三、前端/UI 两阶段工作流]；展示数量策略仅作用于「方向预览」的数量，不作用于是否做预览
2. 非前端场景（FLOW / ARCH / DATA / 以及不含 UI 的 COMPARE）→ 走 [四、非前端可视化流程]
3. 设计说明（文字）→ 仅在对应可视化/方向预览经用户确认后撰写
```

**禁止行为（前端/UI）**（全文唯一列举处）：

- ❌ 仅用 AskQuestion + UI 方向简报（文字 ± 结构图）结束阶段一；用 Mermaid / 决策矩阵 / 结构图替代**方向预览**
- ❌ 未完成方向预览确认前：进入阶段二、写入完整 `design.md`、或宣称「设计已呈现」
- ❌ 跳过阶段一直接询问高保真 Demo
- ❌ 阶段一 AskQuestion 选 Companion/HTML；未按 §七 尝试即写 `direction-*.html`

**命中后动作**：按上表进入第三或第四节 → 完成可视化验收 → Step 5 逐节呈现（UI 另按 [前端设计](#frontend-stack-ui) 确认并记录）→ Step 6 落盘 `design.md` 与 Superpowers spec，归档 `direction-*.html`、高保真 Demo（若有）、结构图说明。

---

## 三、前端/UI 两阶段工作流（场景 `UI` 命中时强制执行）

Agent **不可跳过阶段一**。阶段一**工具不询问**（直接 Companion）；1.2 展示数量、1.4 方向确认、阶段二高保真仍须 AskQuestion。

### 阶段一：方向预览

**目标**：交付至少一份**方向预览**，供用户在浏览器中确认 UI 大方向。

#### 步骤 1.1 — Visual Companion（必选）

按 [§七](#visual-companion-启动备忘windows) 启动 → 向 `screen_dir` 写入预览 → `cursor-ide-browser` 打开 `url`（禁止 `file://`）。**仅**启动/重试仍失败时走 [§七 失败降级](#失败降级)。

#### 步骤 1.2 — 确定展示数量（AskQuestion，仅影响预览份数）

Companion 可用或已降级后，询问用户：

| 模式 | 行为 | 约束 |
|------|------|------|
| **多预览并排** | 2～3 份方向预览（多个 Companion 屏或多个 `direction-*.html`） | 每份可独立辨认；选定后再问阶段二 |
| **单一聚焦** | 仅交付 1 份方向预览 | **其余备选方向只能以差异列表描述**（不得用结构图或长篇文字替代第二、第三份预览） |

**禁止**：将「单一聚焦」理解为「一个 Mermaid + 多段文字描述各备选方向」。

#### 步骤 1.3 — 收集方向约束

须明确至少以下维度（可与预览迭代合并）：

1. 整体布局风格（侧边栏 / 顶栏 / 卡片网格 / 单页滚动等）
2. 核心组件结构（表单、列表-详情、弹窗 vs 内联、Tab vs 跳转等）
3. 关键交互行为（保存时机、刷新范围、批量操作等）
4. 响应式策略（桌面优先 / 移动优先 / 兼顾）
5. 色调与视觉风格（浅/深、品牌色、简约/丰富）

维度确认结果写入 **UI 方向简报**（文字，可附结构图），但**简报不能替代步骤 1.1 的方向预览**。

#### 步骤 1.4 — 用户确认（硬性关卡）

须获得用户对**方向预览**的明确确认。**未确认前禁止**进入阶段二；其余门禁见 [路由与禁止](#路由与禁止)。

---

### 阶段二：高保真 Demo

阶段一确认后，Agent **必须**询问：

> 方向预览已确认。是否需要制作**高保真 Demo 页面**？（将写入 `docs/<branch>/demo/`，更接近最终实现风格）

| 用户回答 | Agent 行为 |
|----------|------------|
| **需要** | 先确认 [前端设计](#frontend-stack-ui) → 按 `design.md` 读取 `frontend-design` 并按其路由 → 写入 `docs/<branch>/demo/`（页顶标注分支名与功能名；勿与 `direction-*.html` 混淆）→ [八、Demo 文件本地预览](#八demo-文件本地预览) 验收 |
| **不需要** | 不制作高保真 Demo；归档阶段一的方向预览与 UI 方向简报 |

**阶段二不能替代阶段一**：高保真 Demo 是增量，不是「第一次可视化」。

### 前端设计（场景 UI） {#frontend-stack-ui}

- **时机**：进入阶段二，或撰写 `design.md` 前——若尚无「前端设计」节 → **AskQuestion**：美学模式（开放式 / 参考式；参考式须选 slug，见 `frontend-design` 参考索引）、UI 实现方式（至少纯 HTML/CSS/JS、antd；库见 `frontend-design` 约束式子技能表）。
- **记录**：写入 `design.md`「前端设计」节（格式见 `doc-file-definition-design`）；项目级定稿在 `/accept` 合入时写入 `docs/ARCHITECTURE.md` §8（见 `doc-file-definition-architecture`）。
- **1-N**：`docs/ARCHITECTURE.md` §8 已存在且本次不变更 UI 规范时，可不重复全套 AskQuestion（用户明确要求变更时除外）。
- **高保真 Demo / 实现**：以 `design.md` 为准读取 `frontend-design`；**禁止**因无 `package.json` 静默默认为纯 HTML。

---

## 四、非前端可视化流程（场景 `FLOW` / `ARCH` / `DATA`，或不含 `UI` 的 `COMPARE`）

### 4.1 输出形式

| 场景 | 工具 | 交付物 | 位置 |
|------|------|--------|------|
| `FLOW` | Mermaid | 流程图 / 泳道图 | 对话中渲染 |
| `ARCH` | Mermaid | 架构图 / 数据流图 / 时序图 | 对话中渲染 |
| `DATA` | Mermaid | ER 图 | 对话中渲染 |
| `COMPARE`（非 UI） | Mermaid / Canvas | 决策矩阵、对比图 | 对话中或 Canvas |

### 4.2 展示数量策略

进入可视化前 AskQuestion：

| 模式 | 行为 |
|------|------|
| **多图并排** | 同时输出 2～3 个 Mermaid/Canvas 变体，供横向比对 |
| **单一聚焦** | 输出 1 个结构图；其余备选以文字差异列表描述 |

复杂 `ARCH` 可分层次：先全景图 → 再局部放大（仍属「单一聚焦」的分步展示）。

### 4.3 用户确认后

输出设计说明；`COMPARE` 的结论写入 `design.md`。

---

## 五、场景叠加：`UI` + `COMPARE`

同时命中：**仍走 [三、阶段一](#阶段一方向预览必选验收门槛)**（禁止见 [路由与禁止](#路由与禁止)）；预览确认后可另用结构图/表格补充非布局类对比（如性能、成本）。

---

## 六、规范附录

### Mermaid 图表规范（`FLOW` / `ARCH` / `DATA` / 非 UI 的 `COMPARE`）

- 流程图：`flowchart` / `graph`；泳道：`journey` / `gantt`（带参与者）
- ER：`erDiagram`；架构：`graph`；时序：`sequenceDiagram`
- 节点使用中文；标出关键决策点（菱形）；必要时加图例

### Canvas 规范（可选）

复杂数据分析或非 UI 的 `COMPARE` 可使用 Canvas（`.canvas.tsx`）。

---

## 七、Visual Companion

场景 `UI` 阶段一主路径；须完成 1.4 用户确认。

### Visual Companion 启动备忘（Windows） {#visual-companion-启动备忘windows}

Superpowers 自带 `visual-companion.md` 以 macOS/Linux 为主；在 **Windows** 上 Agent 须按本节执行，否则极易出现退出码 `127`、`.superpowers/brainstorm/` 未生成、Companion 静默失败。

#### 根因（必读）

| 现象 | 原因 |
|------|------|
| `bash ... start-server.sh` → 退出码 **127** | `C:\Windows\System32\bash.exe` 是 **WSL 启动器**，不是 Git Bash |
| `No such file or directory`（路径明明存在） | WSL 内 **无法识别** `C:/Users/...` 形式的 Windows 路径 |
| 脚本未执行 | 未出现 `test-project/<name>/.superpowers/brainstorm/<session>/` |

**禁止**：在未确认 shell 类型的情况下，写 `bash "C:/Users/.../start-server.sh"` 并假定会成功。

#### 推荐启动方式（按优先级）

**方式 1 — Git Bash + `--background`（Agent 首选）**

- 使用 **Git Bash 可执行文件**，勿用 System32 的 `bash`：

```powershell
& "C:\Program Files\Git\bin\bash.exe" -lc `
  "'/c/Users/<user>/.cursor/plugins/local/superpowers/skills/brainstorming/scripts/start-server.sh' --project-dir '/d/code/cursor/my_harness/test-project/<name>' --background"
```

- 路径约定（Git Bash / MSYS）：
  - `C:\` → `/c/`
  - `D:\` → `/d/`
- Agent 调用 Shell 时对该命令使用 **后台执行**（等价 Superpowers 文档中的 `run_in_background: true`）
- 从 stdout 或 `<project>/.superpowers/brainstorm/<session>/state/server-info` 读取 JSON，保存 `url`、`screen_dir`、`state_dir`

**方式 2 — WSL + `/mnt/<盘符>` 路径**

```powershell
wsl bash /mnt/c/Users/<user>/.cursor/plugins/local/superpowers/skills/brainstorming/scripts/start-server.sh `
  --project-dir /mnt/d/code/cursor/my_harness/test-project/<name> --background
```

- 必须使用 **`/mnt/c/...`**，禁止 `C:/...`

#### 启动后 Agent 流程

1. 确认 `state/server-info` 存在；若无或存在 `server-stopped`，按上文方式 **重试启动**（Companion 空闲约 30 分钟会自动退出）
2. 用 **Write** 向 `screen_dir` 写入新的 `*.html`（**内容片段**即可，见 Superpowers `visual-companion.md`；勿复用文件名）
3. 在 **cursor-ide-browser** 中 `browser_navigate` 到 `url`（**禁止** `file://`）
4. 用户反馈后读取 `state/events`（若存在）并与终端消息合并
5. 下一问若不需浏览器，推送 `waiting.html` 清屏
6. 会话结束可执行 Superpowers `stop-server.sh`（同样须用 Git Bash 或 WSL 正确路径）

#### 失败降级 {#失败降级}

Git Bash 与 WSL 均无法启动时：**告知原因** → 写 `docs/<branch>/demo/direction-<简述>.html`（页顶标注分支/功能/方向号）→ [§八](#八demo-文件本地预览) `preview-html`。**勿** AskQuestion 是否改用 HTML；**勿**未尝试即降级或仍称已用 Companion。

---

## 八、Demo 文件本地预览

阶段一降级、阶段二 Demo 及 `docs/<branch>/demo/` 下 HTML：执行 **`preview-html`** + 内置浏览器（禁止 `file://`）。Companion 见 [§七](#visual-companion-启动备忘windows)。

---

## 九、终态门禁（宣称「设计已呈现」前）

**场景 `UI`：**

- [ ] 阶段一：Companion 已按 [§七](#visual-companion-启动备忘windows) 验收，或已记录降级且 [§八](#八demo-文件本地预览) 验收
- [ ] 已询问高保真 Demo（要/不要均须问）；`design.md`「前端设计」与记录一致；若变更项目级 UI 规范，合入前 `ARCHITECTURE.md` §8 须与 `design.md` 一致

**非 `UI`：**

- [ ] 结构图已交付并获用户确认；设计说明在可视化之后
