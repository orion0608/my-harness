---
description: 经验捕获流程规则——当 Agent 在开发过程中发现有意义的问题时，记录问题和解决方案，累计达到阈值后自动提炼为独立技能并纳入 Harness 体系。
alwaysApply: true
---

# 经验捕获规则

## 核心机制

将开发过程中反复出现的有意义问题系统化捕获。同一问题首次出现时记录到问题清单；重复出现时累加计数；当发现次数达到阈值（3 次）时，自动将解决方案提炼为独立技能，纳入 Harness 体系。

---

## 一、"有意义"的判断标准

以下四类问题视为有意义，应当记录：

| 维度 | 说明 | 示例 |
|------|------|------|
| 上下文浪费 | 问题导致 Agent 大量重复理解上下文，或用户需反复描述同一诉求 | Agent 多次误解同一类指令结构、用户反复解释同一概念 |
| 生产效率 | 问题的解决能显著减少开发耗时或减少返工 | 某个配置错误导致多次部署失败、某个默认行为需频繁手动调整 |
| 代码质量 | 问题的解决能系统性避免某类 bug 或架构缺陷 | 某类边界条件反复遗漏、某项检查每次 Code Review 都指出 |
| 部署效率 | 问题的解决能减少部署失败率或缩短部署耗时 | 某环境差异反复引发线上问题、某个构建步骤频繁卡住 |

Agent 应在会话过程中主动识别符合以上标准的问题，并向用户提议记录。提出时有义务说明该问题符合哪条标准。

---

## 二、问题清单

### 存储位置

`.cursor/skills/lesson-record/lesson-record.md`

### 表格结构

| 列名 | 说明 | 提炼后变化 |
|------|------|-----------|
| 问题编号 | 自增 ID，格式 `Lxxx`（L001 起，三位数字，不足补零） | 不变 |
| 问题描述 | 一句话概括问题，简洁、可检索 | 不变 |
| 首次发现时间 | 格式 `YYYY-MM-DD HH:MM` | 不变 |
| 发现次数 | 累计出现次数（正整数） | 保留最终值 |
| 解决方式 | 具体解决方案，详细记录步骤和要点 | **清空**（迁入提炼技能） |
| 是否提炼为技能 | Y / N | → Y |
| 提炼后的技能名 | 技能目录名（如 `experience-summary-skill/xxx/SKILL.md`） | 填入 |

---

## 三、操作流程

### 3.1 首次记录

当 Agent 在会话中发现有意义的问题时，执行以下步骤：

1. 检查 `lesson-record.md` 问题清单，确认该问题是否已存在
2. 若不存在：
   - 生成新编号（当前最大编号 +1，首个为 L001）
   - 新增一行：填写问题编号、问题描述、首次发现时间、发现次数=1、解决方式、是否提炼=N
3. 若已存在：按 3.2 重复记录流程处理

### 3.2 重复记录

1. 在问题清单中找到对应行
2. 将"发现次数" +1
3. 计算新次数：
   - **次数 < 3**：仅更新次数，不更新解决方式（除非解决方式有改进，则追加）
   - **次数 = 3**：触发提炼流程（见 3.3）
   - **次数 > 3**：仅更新次数（已提炼的问题不再维护解决方式列）

### 3.3 提炼流程（次数 = 3 时触发）

当某个问题的发现次数累计达到 3 次时，执行以下步骤：

1. **创建技能目录**：在 `.cursor/skills/lesson-record/experience-summary-skill/` 下创建以问题特征命名的子目录
2. **创建 `SKILL.md`**：在该子目录下创建 `SKILL.md`，包含：
   - YAML frontmatter：`name`、`description`、`disable-model-invocation: true`
   - 问题背景
   - 根因分析
   - 解决方案（将 `lesson-record.md` 中解决方式的内容迁移至此并细化）
   - 适用场景
3. **更新问题清单**：
   - 将"解决方式"列清空（方案已迁入技能文件）
   - 将"是否提炼为技能"改为 Y
   - 将"提炼后的技能名"填入技能目录名
4. **纳入 Harness 体系**：
   - 在 `.cursor/rules/` 下已有的相关规则中引用该技能（如适用）
   - 更新 README.md 的架构总览和技能表

---

## 四、提炼技能格式规范

```markdown
---
name: <skill-name>
description: <一句话描述该技能解决什么问题>
disable-model-invocation: true
---

# <技能名>

## 问题背景

<!-- 原问题的完整描述，发生了什么，影响是什么 -->

## 根因分析

<!-- 为什么反复发生，深层原因 -->

## 解决方案

<!-- 详细的解决步骤、配置、代码模式等 -->

## 适用场景

<!-- 什么情况下应使用此技能，什么情况下不适用 -->
```

---

## 五、Agent 行为准则

1. **主动识别**：每次会话结束时自我检查，本次对话中是否有符合"有意义"标准的问题
2. **提议记录**：向用户简要说明问题及符合的标准，询问是否记录
3. **勿强行记录**：用户可选择不记录；若用户明确拒绝，不追问
4. **达到阈值主动提炼**：当某问题次数 = 3 时，主动告知用户并执行提炼
5. **格式严格**：编号、日期格式、表格结构不得自行变更
