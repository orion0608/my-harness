# /go

接续执行。用于 Agent 执行异常中断后，指示 Agent 从断点继续完成未竟任务。

## Agent 行为

1. 回顾当前对话上下文，识别中断前正在执行的任务和目标
2. 检查中断前的产物（文件变更、文档记录、Git 状态等），确认当前进度
3. 按原有流程和规则继续执行，无需用户重复描述需求
4. 如无法确定中断点，使用 **AskQuestion** 工具向用户确认接续位置
